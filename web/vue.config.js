module.exports = {
    outputDir: __dirname + '/../server/web', // 输出地址
    // 生成的静态文件路径
    // 如果是生产环境，则生成到 /admin，如果是开发环境，则生成到 /
    /* publicPath: process.env.NODE_ENV === 'production' ? '/web' : '/' */
}